{%- liquid
  assign _p = product
  assign _is_serie_ii = false
  for _col in _p.collections
    if _col.handle == 'serie-2' or _col.handle == 'serie-ii'
      assign _is_serie_ii = true
      break
    endif
  endfor
-%}
{%- if _is_serie_ii -%}
  <p class="vf-edition-statement">Numbered edition</p>
{%- endif -%}
